import React from 'react'

export default function Primeiro() {
    const msg = 'Seja bem vindo(a) !'
    // Utilizar parentêses para deixar o código identado em várias linhas
    return (
        <div>
            <h2>Primeiro Componente</h2>
            <p>{ msg }</p>
        </div>
    )
}

// Test